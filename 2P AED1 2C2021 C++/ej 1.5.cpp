// dado un mapa, queremos devolver un bool q indique si hay al menos dos posiciones
// c misma altura y misma poblaciones

a == b && c == d

esValle

bool existeIgual(mapa m, posicion p){
    bool res = false;

    for(...){
        for(...){
            if (!(i == p0 && j == p1))
                res = res || m[i][j] == m[p0][p1];
        }
    }

    return res;
}



bool elias(mapa m){
    bool res = false;

    for(...){
        for(...){
            posicion posActual = <i,j>;
            res = res || existeIgual(m, p);
            
        }
    }

    return res;
}